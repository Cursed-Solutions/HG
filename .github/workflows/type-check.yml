name: Type check

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  type-check:
    uses: FasterSpeeding/Piped/.github/workflows/type-check.yml@7f5789a5b500b5ea084370b88f9ff7716de03d65
