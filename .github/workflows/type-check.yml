name: Type check

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  type-check:
    uses: FasterSpeeding/Piped/.github/workflows/type-check.yml@d19d7341ef655ff54d8c6f7bcd92c544872944a5
