name: Type check

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  type-check:
    uses: FasterSpeeding/Piped/.github/workflows/type-check.yml@0af587d7dd3dbda851dca9b36704a8913ce9b410
